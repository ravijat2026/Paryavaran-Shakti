import React from 'react';

const Footer = () => {
  const currentYear = new Date().getFullYear();

  return (
    <div className="min-w-full min-h-[50vh] bg-gradient-to-tl from-[#6bd2d3] to-[#91daea] flex justify-center items-center">
      Footer
    </div>
  );
};

export default Footer;
